type ownProps = {
  domain: string,
};

export default function BoardListPage({ domain }: ownProps) {

  return (<div className="flex" style={{ justifyContent: 'center', alignItems: 'center' }}>
    <h2>방제</h2>


  </div>);
}